# AI Engineer Suite Service Modules

`backend/app/services/ai_stack/ai_engineer_suite_service.py` is the stable import facade.
The implementation lives here in named modules grouped by service concern.

Keep filenames descriptive. Avoid numeric prefixes, generic continuation names, MVP acronyms, and source-string loaders.

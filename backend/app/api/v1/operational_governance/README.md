# Operational Governance API Modules

`backend/app/api/v1/operational_governance_routes.py` is the stable import
anchor for Flask route registration. The implementation lives here in named
modules grouped by operator concern.

Keep filenames descriptive. Avoid numeric prefixes, generic continuation names,
and source-string loaders.

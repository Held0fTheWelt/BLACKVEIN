# Explicit test dependencies for reproducible, clean-environment validation.
#
# Install (choose one — pip resolves "-r requirements.txt" relative to THIS file's directory):
#
#   From repository root:
#     python -m pip install -r backend/requirements-test.txt
#
#   From backend/:
#     cd backend && python -m pip install -r requirements-test.txt
#
# The first non-comment directive includes production dependencies from the same
# directory as this file. You do not need a separate "pip install requirements.txt"
# step when using this file.
#
# Does NOT include development-only tools (formatters, linters, type checkers).
# Those are in requirements-dev.txt.

-r requirements.txt

# ====== APP UNDER TEST (explicit anchor) ======
# Backend pytest loads `app` (Flask). Declared here too so a grep of this file shows
# the runtime dependency even though Flask is already pulled in via -r requirements.txt.
flask>=3.0.6,<4

# ====== TEST EXECUTION (REQUIRED) ======
# Minimal packages to run the backend pytest suite in CI, containers, and fresh clones.

pytest>=7.0,<9
pytest-asyncio>=1.3,<2
pytest-cov>=4.0,<6
pytest-timeout>=2.1
# Optional opt-in parallel execution (Stage 3 backend-test-suite-split). Activated only
# when ``tests/run_tests.py --parallel [auto|N]`` is passed; default behavior is sequential.
pytest-xdist>=3.5,<4
anyio>=4.0,<5
exceptiongroup>=1.2.0; python_version < "3.11"

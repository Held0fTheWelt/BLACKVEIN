[project]
name = "story-runtime-core"
version = "0.1.0"
description = "Shared canonical story runtime core for World of Shadows."
requires-python = ">=3.11"
dependencies = [
  "pydantic>=2.6",
  "httpx>=0.27",
]

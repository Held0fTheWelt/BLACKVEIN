# Story Runtime Shell Readout Modules

`world-engine/app/story_runtime_shell_readout.py` is the stable public facade.
The implementation lives here in named modules grouped by readout concern.

Keep filenames descriptive. Avoid numeric prefixes, generic continuation names, MVP acronyms, and source-string loaders.

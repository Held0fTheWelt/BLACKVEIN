services:
  backend:
    build:
      context: ./Backend
      dockerfile: Dockerfile
    ports:
      - "8000:8000"
    env_file: .env
    volumes:
      - ./Backend/instance:/app/instance
    environment:
      - FLASK_APP=run:app

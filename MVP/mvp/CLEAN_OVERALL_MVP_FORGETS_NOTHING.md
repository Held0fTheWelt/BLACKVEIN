# World of Shadows — Clean Overall MVP (Nothing Forgotten)

## 1. Executive position

This repository contains **one current MVP**, not many competing MVPs.

The current MVP is the **real integrated directory state** and must be treated as the source of truth.
All wave reports, validation summaries, intermediate ZIPs, and old planning files are:

- supporting evidence,
- implementation history,
- closure artifacts,
- or stale/partial planning surfaces,

but they are **not separate current MVPs**.

---

## 2. The critical correction

The previously discussed `Task.md` plan is **not the full current MVP**.

It is a **historical implementation plan** for one major integration track:

- A — Runtime Authority
- B — MCP Surface
- C — Player Routes
- D — AI Stack Integration
- E — Governance Surfaces
- F — Validation & Release

However, this must now be read carefully:

### What is true
- The historical plan was a real implementation plan.
- Under the conditions and scope of that plan, **A–D were worked through**.

### What is not true
- A–D being worked through under that old plan does **not** mean the entire current MVP can be described by that plan.
- It also does **not** mean the old plan alone captures the much richer current repository state.

So the correct conclusion is:

> The old plan is part of the MVP history, but **not the whole current MVP**.

---

## 3. What the current MVP now includes

The real current MVP includes **all of the following layers together**.

### Layer I — Historical integration spine (old plan track)
This is the repository/product integration line originally described by `Task.md`.

#### Historical Workstream A
Runtime authority / world-engine truth

#### Historical Workstream B
MCP surface / gated tool access / AI integration seams

#### Historical Workstream C
Player routes / player interaction with the game world

#### Historical Workstream D
AI stack integration

### Important status note
These are treated as **historically worked through under the old plan’s conditions**.
They are part of the current MVP, but the current MVP also extends well beyond them.

---

## 4. Runtime / session / UI / shell proofment is part of the MVP

The current MVP is not just a backend/runtime skeleton.

It also includes a real player-facing runtime/session line with proof-bearing work around:

- authoritative world-engine session truth,
- backend session bridge,
- frontend play shell,
- execute / observe / transcript continuity,
- runtime-to-shell readout integration,
- player-facing surfacing of live social/dramatic state.

This means the MVP already covers **real runtime truth flowing into actual player-facing session experience**.

Key real seams visible in the current directory include:

- `world-engine/app/story_runtime/manager.py`
- `world-engine/app/story_runtime_shell_readout.py`
- `backend/app/api/v1/session_routes.py`
- `backend/tests/test_session_routes.py`
- `frontend/app/routes.py`
- `frontend/tests/test_routes_extended.py`
- `world-engine/tests/test_story_runtime_shell_readout.py`

---

## 5. Canonical Experience is part of the current MVP

The current MVP also includes the God of Carnage Canonical Experience hardening line.

This is not a separate side project anymore.
It is part of the current MVP state.

That includes at least these families of work:

- social reading
- semantic move interpretation
- runtime response shaping
- visible response/output hardening
- domestic-action absorption
- role-sensitive readings
- carryover / aftershock / reuse
- room/object anchoring
- player-facing observation / transcript hardening

The MVP is therefore not just “engine + routes”.
It already includes a serious **God of Carnage experience layer** on top of the runtime spine.

---

## 6. A-line status (player-facing response / shell / anchoring hardening)

The current MVP includes the A-line hardening work and its closure path.

The accessible validation surfaces show this line through at least:

- AE — Authoritative Runtime + Shell Integration
- AF — Readout Precision Hardening
- AG — Situational Affordance Surfacing
- AH — Role-Sensitive Consequence Readout Precision
- AI — Social-Axis Geometry Readout
- AJ — Contextual Readout Compression + Salience Gating
- AK — Delta-Focused Reaction Readout
- AL — In-Situation Turn Framing + Address Pressure
- AM — Character-Addressed Response + Observation
- AN / AN2 — True Character-Addressed Runtime Response
- AO — Pair/Side Voice Differentiation
- AP — Exchange-Type Differentiation + Response-Cause Compression
- AQ — Carryover-in-Response
- AR-P — Reconciled Prior-Line Reuse Truth Position
- AS — Room/Object Response Anchoring
- AT — Domestic Surface Heat
- AU — Surface Priority Compression
- AV / AW / AX — later visible-response refinements
- AY — Final A-line closure audit

### A-line meaning in practical terms
The player-facing session layer now materially supports:

- non-directive situational readout,
- live social pressure readability,
- side/pair/exchange-type differentiation,
- carryover and prior-line reuse,
- room/object anchoring,
- compact but real observation/transcript hardening.

### A-line status
**A-line is part of the current MVP and is treated as closed at its intended level for the current package truth.**

---

## 7. B-line status (Character Depth / Social Performance)

The current MVP also includes B-line work.

That means the package already goes beyond shell/readout quality and into:
- character voice differentiation,
- defensive style differentiation,
- mask-slip / self-presentation,
- character-led recentering,
- stronger figure-specific social performance.

Accessible validation surfaces show:
- BA is present
- B-line closure audit is present
- the package truth calls B-line materially achieved for the current package slice

### B-line meaning in practice
The response layer is not only socially informative.
It is more clearly carried by:
- who this person is,
- how they defend themself,
- how their social mask slips,
- and how they try to pull the room back under their own terms.

### B-line status
**B-line is already materially part of the current MVP.**

---

## 8. C-line status (reply continuity / delayed reuse)

The current MVP includes the C-line.

This line hardens:
- multi-turn reply continuity,
- delayed visible reuse,
- answer-afterlife across turns,
- stronger visible continuity in the actual response/output path.

Accessible validation surfaces show:
- C1 is present
- C2 is present
- C-line closure summary is present

### C-line status
**C-line is materially part of the current MVP and is treated as achieved for the current package slice.**

---

## 9. D-line status (interruption / hesitation / reentry pressure)

The current MVP includes the D-line.

This line hardens:
- interruption,
- hesitation,
- dodge / deflection,
- reentry pressure,
- answer rhythm under tension.

Accessible validation surfaces show:
- D1 is present
- D2 is present
- D-line closure summary is present

### D-line status
**D-line is materially part of the current MVP and is treated as achieved for the current package slice.**

---

## 10. E-line status (micro-weave exchange choreography)

The current MVP includes the E-line.

This line hardens:
- short exchange choreography,
- reopen-through-dodge behavior,
- multi-reply weave pressure,
- stronger local conversational shape.

Accessible validation surfaces show:
- E1 is present
- E-line closure summary is present

### E-line status
**E-line is materially part of the current MVP and is treated as achieved for the current package slice.**

---

## 11. F-line status (multi-speaker pressure relay)

The current MVP includes the F-line.

This line hardens:
- two-speaker relay,
- three-speaker pressure relay,
- same-surface pressure jumping speakers,
- stronger multi-speaker pressure carrying.

Accessible validation surfaces show:
- F1 is present
- F-line closure summary is present

### F-line status
**F-line is materially part of the current MVP and is treated as achieved for the current package slice.**

---

## 12. Validation / closure / truth layer is part of the MVP

The current MVP also includes a strong proof-and-truth layer.

That includes:
- engine proof
- backend proof
- frontend shell-loop proof
- activation proof
- vertical-slice hardening proof
- wave summaries
- line closure audits
- reconciliation artifacts
- current-state validation summaries

This proof layer is not “extra documentation”.
It is part of how the MVP is carried and supervised honestly.

---

## 13. What the current MVP is, in one sentence

The current MVP is:

> **an integrated MVP-v24 repository state that already combines the historical integration spine, authoritative runtime/session truth, player-facing shell/readout proofment, and a materially advanced God of Carnage Canonical Experience hardening stack through A/B/C/D/E/F experience lines plus package-level validation truth.**

---

## 14. What should no longer be forgotten

Any clean overall MVP description must explicitly remember all of these:

### Must remember
- there is **one current MVP**
- `Task.md` is historical and partial
- historical A–D were worked through under the old plan’s conditions
- runtime/session/UI/shell proofment is already part of the MVP
- Canonical Experience hardening is already part of the MVP
- A-line is already part of the MVP
- B-line is already part of the MVP
- C-line is already part of the MVP
- D-line is already part of the MVP
- E-line is already part of the MVP
- F-line is already part of the MVP
- proof / validation / reconciliation artifacts are part of the MVP truth layer

### Must not do
- treat old plan stages as the whole MVP
- treat wave artifacts as separate competing MVPs
- forget the runtime/UI/player-facing proof line
- forget the experience-hardening line
- confuse historical workstream completion with full modern current-state closure

---

## 15. What is still allowed to remain open

This document does **not** claim that nothing in the entire repository can ever be improved.

It only states the clean current MVP boundary.

Future work may still exist above this MVP, for example:
- new follow-on hardening lines
- broader cleanup of stale truth files
- packaging cleanup
- future post-MVP experience work

But those are **follow-on work**, not evidence that this current integrated MVP is incomplete as a concept.

---

## 16. Clean planning consequence

From now on, planning should use this order of truth:

1. **the current directory state**
2. **the current validation / closure / current-state files**
3. **then only after that, historical plans like `Task.md`**

That prevents the old partial plan from under-describing the real MVP again.

---

## 17. Final corrected MVP position

The clean overall MVP is:

- **one current integrated MVP**
- **not** the old `Task.md` plan alone
- **not** one isolated wave artifact
- **not** many competing versions

It already includes:

- the historical integration spine,
- runtime/session/player-shell proofment,
- Canonical Experience hardening,
- A-line closure-level response/readout/anchoring work,
- B-line character/social-performance hardening,
- C-line delayed reply continuity,
- D-line interruption/hesitation/reentry pressure,
- E-line micro-weave exchange choreography,
- F-line multi-speaker pressure relay,
- and the package-level proof / closure / reconciliation layer.

That is the MVP that should now be treated as the real current master state.

{
  "mcpServers": {
    "wos-mcp": {
      "type": "stdio",
      "command": "python",
      "args": ["-m", "tools.mcp_server.server"],
      "description": "World of Shadows MCP Server - Game runtime, content, and capabilities",
      "env": {
        "BACKEND_BASE_URL": "http://localhost:8000",
        "WOS_MCP_OPERATING_PROFILE": "healthy"
      }
    }
  }
}
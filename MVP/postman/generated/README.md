# `postman/generated/`

The **full OpenAPI Postman export** is kept at the **`postman/`** root as:

- **`../WorldOfShadows_Complete_OpenAPI.postman_collection.json`**

Postmanify’s default **`generate`** writes the master file there (not in this folder). This directory is reserved for optional future artifacts (reports, Newman output, etc.).

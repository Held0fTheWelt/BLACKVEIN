# Contractify superpowers (Cursor router skills)

Canonical skills live in subfolders: ``<skill-name>/SKILL.md``.

After edits, from the repository root:

```bash
python "./'fy'-suites/contractify/tools/sync_contractify_skills.py"
```

Path validation:

```bash
python "./'fy'-suites/contractify/tools/validate_contractify_skill_paths.py"
```

Committed mirrors: ``.cursor/skills/<skill-name>/SKILL.md``.

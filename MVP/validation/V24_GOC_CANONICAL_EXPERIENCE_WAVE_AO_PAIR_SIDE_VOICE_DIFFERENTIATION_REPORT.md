# V24 GoC Canonical Experience — Wave AO Pair/Side Voice Differentiation Hardening

## Scope

This pass hardens the actual runtime observation/response layer so the visible response feels more clearly carried by:
- the host side
- the guest side
- the spouse axis
- cross-couple strain

The pass remains narrow and additive. It does not redesign architecture and does not rely primarily on extra shell-only metadata.

## What changed

Runtime-side response framing was strengthened in `world-engine/app/story_runtime_shell_readout.py`.

The response framing now differentiates:
- responder side (`host side` / `guest side`)
- pair/pressure line (`spouse-axis embarrassment`, `cross-couple strain`, etc.)
- why the reply belongs to the current pressure configuration

The runtime-framed response then continues to feed:
- `visible_output_bundle_addressed`
- frontend transcript framing
- latest observation line / transcript preview

## Validation rerun

Focused proof was rerun across:
- world-engine shell-readout tests
- backend session route bridge tests
- frontend play-shell observation/readout tests
- narrow ai_stack GoC meaning tests

## Result

Wave AO is closed.

The actual response/output flow is now more clearly differentiated by side / pair / pressure line, while remaining compact, non-directive, and canon-corridor-preserving.

# Optional documentation site build (MkDocs Material).
# Install: pip install -r requirements-docs.txt
# Build:   mkdocs build
# Serve:  mkdocs serve

mkdocs-material>=9.5.0,<10
pymdown-extensions>=10.0,<11

# Backward Compatibility

- Command envelopes preserve read compatibility for the prior supported schema version.
- Manifest support is explicitly version-bounded.
- Breaking contract changes require changelog and deprecation notice entries before release.

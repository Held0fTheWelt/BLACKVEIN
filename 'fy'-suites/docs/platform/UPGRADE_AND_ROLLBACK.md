# Upgrade and Rollback

- Create a managed backup before schema-affecting or contract-affecting upgrades.
- Roll back explicitly from the latest or selected backup when recovery is required.
- Keep schema state and migration evidence in `.fydata/registry/`.

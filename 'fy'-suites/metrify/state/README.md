# Metrify state

- `ledger.jsonl` is the append-only internal cost ledger.
- `latest_summary.json` is the current roll-up used by reports and AI packs.
- `METRIFY_STATE.md` is the human-readable current state.

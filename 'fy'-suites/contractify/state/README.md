# Contractify `state/`

Human/session notes and **pre-work snapshots** live here. The audit CLI writes JSON to `../reports/` by default.

Machine-readable drift and discovery: prefer `reports/contract_audit.json`.

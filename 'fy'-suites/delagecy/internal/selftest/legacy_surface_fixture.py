"""Fixture used by delagecy self-scans.

The word legacy appears here intentionally so the suite can verify that
its scanner handles its own control surfaces when explicitly included.
"""

LEGACY_FIXTURE_LABEL = "legacy self-test fixture"

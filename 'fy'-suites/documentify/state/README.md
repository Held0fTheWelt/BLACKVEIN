# Documentify state

- `DOCUMENTATION_STATE.md` is the stable tracked state file.
- `generated/` contains the current drafted documentation outputs.
- `reports/` contains machine + markdown generation summaries.

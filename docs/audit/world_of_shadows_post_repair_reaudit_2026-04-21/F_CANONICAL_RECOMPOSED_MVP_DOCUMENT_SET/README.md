# Canonical recomposed MVP document set — post-repair delta

The repaired package already has a strong active canonical route.
This folder does **not** replace that route with another large rewrite.

It provides the **minimal recomposition deltas** required after the post-repair re-audit:

- normalize the docs root to the active route,
- normalize the audit root to the current repair/evidence lane,
- remove remaining semantic competition between the detailed companion README and the active route,
- and normalize the proof-posture doc’s “current evidence route”.

# Runtime-proof task list

The final canonical package is stronger and more complete than any single supplied input, but it is still **not honestly the end of runtime-proof work for the whole platform**.

These tasks describe what still needs to happen before a broader runtime-proof claim would be justified.

| Task | Proof target | Why | Expected evidence |
|---|---|---|---|
| Provision Flask-capable backend/frontend test environment and rerun targeted route proofs | Replay backend session-route shell/proxy tests and frontend play-shell framing tests in an environment with required Flask dependencies. | Current container blocks these route-level proofs with ModuleNotFoundError: flask. | Fresh PASS outputs for the targeted backend and frontend pytest commands, plus any required environment note. |
| Reproduce the full dual-surface player-path contract | Demonstrate with tests and/or replay evidence which path is canonical for ordinary players, which path is live-room advisory, how they converge, and how audience classification is exposed. | Committed-path vs live-room/player-visible transport discipline remains a bounded proof seam. | Route-level tests or E2E evidence showing no silent truth divergence across player-visible surfaces. |
| Replay publish -> activation -> shell -> re-entry as one end-to-end chain | Fresh evidence that the published artifact identity survives activation, session birth, shell hydration, turn progression, resume, and re-entry. | The chain is strongly source-supported but only partly rerun in this pass. | Integrated route/service/E2E replay with provenance and audience classification visible. |
| Replay preview, evaluation, promotion, rollback, and runtime-health governance flow | Show the governed lifecycle working end to end, including findings, revision candidates, preview isolation, evaluation deltas, promotion decision, history, rollback reachability, and runtime-health interpretation. | Canonical lifecycle truth is restored, but fresh live replay is still incomplete. | Admin/operator-facing replay, logs, and validation outputs covering the lifecycle chain. |
| Provision LangGraph/LangChain dependencies and rerun graph-heavy AI-stack proof | Replay the skipped breadth-playability and fallback/orchestration-heavy AI-stack tests under a fully provisioned environment. | Graph-heavy proof remains environment-limited in this pass. | PASS or bounded-failure outputs for the graph-heavy test set with explicit dependency context. |
| Demonstrate research/improvement and MCP support without truth-boundary drift | Show that research findings, improvement proposals, preview tools, and MCP operator tooling remain visible and useful while still subordinate to publish/runtime authority. | The subsystem is now preserved canonically but not equally product-proven. | Capability-gated flows, traceability, and tests showing no silent canon mutation or second truth boundary. |
| Close backend transitional retirement honestly | Either prove retirement-safe removal of transitional backend session surfaces or reclassify them explicitly with stable compatibility posture. | This remains a named residue in the validation corpus. | Updated code, tests, and audit record showing resolved ownership and no hidden fallback truth path. |
| Attempt evaluator-independent higher-level closure only after the above replays pass | Advance from strong active-slice Level A posture toward a stronger evaluator-independent closure classification. | Current evidence does not justify skipping directly to that claim. | Independent evaluation package or equivalently strong externalized proof corpus tied to the refreshed runtime evidence. |

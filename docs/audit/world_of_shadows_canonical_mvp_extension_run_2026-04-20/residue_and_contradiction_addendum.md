# Residue and contradiction addendum for extension run

## Residues kept visible

### 1. Restored lifecycle truth is broader than fresh container replay
The package lifecycle and governance material is now carried forward correctly, but not fully replayed end to end in this container.

### 2. Graph-heavy fallback/runtime replay remains dependency-sensitive
Some fallback-related runtime surfaces still depend on optional graph/runtime dependencies that are not fully present here.

### 3. Flask-backed backend/frontend/admin proof remains environment-limited
The extension run again hit the Flask dependency boundary for selected route-level replay.

### 4. Settings and control-plane governance remain canonical but not fully freshly productized here
The material is strong enough to preserve, but not strong enough to overclaim as completely replayed polished admin closure in this run.

## No new hard contradiction found

The extension run did not find a new evidence family that contradicted the previous preservation package.
The problem was underrepresentation, not new conflict.

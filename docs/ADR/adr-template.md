# ADR-NNNN: Title

Short title line only in the H1; expand in **Context** / **Decision**.

## Status

`Proposed` | `Accepted` | `Superseded by ADR-XXXX` (use exact ADR id when replacing this record)

## Date

`YYYY-MM-DD` (authorship date of this file; note in **Context** if the decision predates the file)

## Supersedes *(optional)*

ADR ids this decision **explicitly replaces**, if any (e.g. `ADR-0021`). Omit the section entirely when not applicable.

## Intellectual property rights

Repository authorship and licensing: see project **LICENSE**; contact maintainers for clarification.

## Privacy and confidentiality

This ADR contains no personal data. Implementers must follow the repository privacy and confidentiality policies, avoid committing secrets, and document any sensitive data handling in implementation steps.

## Related ADRs *(optional)*

Bullets linking to other `docs/ADR/adr-*.md` files that **must be read together** (tight coupling). Omit when none.

## Context

What forces the decision? Link to incidents, contracts, audits, or roadmap items. If material was **migrated** from an MVP or archive index, summarise it here (do not leave narrative only in `.bak` files).

## Decision

What we will do — **testable** bullets or a single normative paragraph.

## Consequences

**Positive:** …

**Negative / risks:** …

**Follow-ups:** …

## Testing

How we **verify** this decision: CI gates, contract tests, manual checks, or explicit **“not yet automated”**. Note **failure modes** that should trigger an ADR review (e.g. drift in dual surfaces, missing enforcement).

## References

- …

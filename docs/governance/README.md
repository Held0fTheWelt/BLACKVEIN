# Architecture decision records (legacy compatibility path)

The canonical ADR collection for the MVP now lives under [`docs/ADR/`](../ADR/README.md).

This legacy location is retained only so older repository-relative links do not break during the bounded migration.
Do not attach new governance work to `docs/governance/`.

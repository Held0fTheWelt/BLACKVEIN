# MVP definition (canonical redirect anchor)

This path is a **stable architecture-contract anchor** retained for smoke tests,
legacy links, and runtime-proof packaging continuity.

The active canonical document now lives at:

- [`docs/technical/architecture/mvp_definition.md`](../technical/architecture/mvp_definition.md)

## Why this file still exists

World of Shadows has repeatedly moved from broader historical packages into a
leaner canonical structure. During that normalization, several architecture
anchors were relocated under `docs/technical/architecture/`. The runtime-proof
package still needs a durable, human-readable path for older audit bundles,
smoke tests, and operator references that expect `docs/architecture/*` to
resolve.

## Binding meaning

Use the technical-architecture target above as the active normative source for:

- what the MVP currently promises,
- what belongs inside the canonical claim surface,
- and how the runtime-bearing vertical slice is bounded.

This redirect file is intentionally preserved so package truth, repository
truth, and smoke-test truth remain aligned rather than silently drifting apart.

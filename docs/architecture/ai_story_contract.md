# AI story contract (canonical redirect anchor)

This repository keeps this file so architecture references that still target
`docs/architecture/ai_story_contract.md` continue to resolve inside the current
package.

The active normative source is:

- [`docs/technical/architecture/ai_story_contract.md`](../technical/architecture/ai_story_contract.md)

## Role in the runtime-proof surface

The AI story contract remains part of the binding seam between free player
input, AI interpretation/proposal behavior, and the authoritative runtime that
accepts, rejects, or normalizes state-affecting outcomes.

It therefore still matters for:

- proposal versus commit discipline,
- structured output expectations,
- narrative continuity within the GoC slice,
- and proof posture around what AI is allowed to do.

This redirect keeps the claim surface honest by ensuring the historical anchor
continues to resolve to the current contract lane instead of disappearing.

# Deprecations

- `POSTMANIFY-LEGACY-NAME-001`: Legacy default collection naming detected; prefer manifest-driven generic naming.
  - replacement: `Configure suites.postmanify.out_master in fy-manifest.yaml`
  - removal_target: `wave-2`
